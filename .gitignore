.test-workstreams/
.DS_Store
.orbitdock-worktrees/
*.db
*.db-wal
*.db-shm

# Certificates (NEVER commit these)
*.p12
*.cer

# Build artifacts
*.zip
.cache/
# Docker Buildx local cache (Linux release builds)
.cache/docker-buildx/

# Xcode
*.xcworkspace/
buildServer.json
# Note: Package.resolved is force-tracked (git add -f) for CI reproducibility
xcuserdata/
.claude/

# Swift Package Manager
.build/
.swiftpm/

# Node.js (Vizzly CLI)
node_modules/

# Claude Agent SDK local install (source-of-truth, but generated)
orbitdock-server/docs/node_modules/
orbitdock-server/docs/claude-agent-sdk-version.json
orbitdock-server/docs/claude-agent-sdk-*.md

# Vizzly
.vizzly/

# Test results
*.xcresult
*.trace/
.logs/
test-results/

# Release artifacts
dist/
files/artifacts/
