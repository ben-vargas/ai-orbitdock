.test-workstreams/
*.db
*.db-wal
*.db-shm

# Certificates (NEVER commit these)
*.p12
*.cer

# Build artifacts
*.zip
.cache/

# Internal protocol docs (keep locally, don't publish)
orbitdock-server/docs/claude-agent-sdk-protocol.md

# Xcode
*.xcworkspace/
buildServer.json
# Note: Package.resolved is force-tracked (git add -f) for CI reproducibility
xcuserdata/
.claude/

# Swift Package Manager
.build/
.swiftpm/

# Node.js (Vizzly CLI)
node_modules/

# Vizzly
.vizzly/

# Test results
*.xcresult
*.trace/
.logs/

# Whisper model (fetched locally for app bundling)
OrbitDock/OrbitDock/WhisperModels/*.bin

# Release artifacts
dist/
